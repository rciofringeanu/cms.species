<?php

/**
 * Default renderer for the national reports table inside the country profile page
 * @param $variables
 * @return string
 */
function theme_table__species_template_country($variables) {
    return theme_table($variables);
}

function theme_table__species_template_instrument($variables) {
    return theme_table($variables);
}
